#include <stdio.h>

int main() {
  char a = 'B';

  printf("Decimal of a = %d \n", a);

  if (a == 'B')
    printf ("Value of a = a is (%c)\n", a);
  else
    printf ("Value of a != a is (%c)\n", a);
  
  return 0;
}
